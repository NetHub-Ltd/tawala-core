ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Credentials only — app builds async + sync DSNs (asyncpg / psycopg).
# Replace placeholders; do not commit real secrets.
DB_HOST=localhost
DB_PORT=5432
DB_USER=core_user
DB_PASSWORD=
DB_NAME=tawala_core
